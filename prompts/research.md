---
description: Look up external docs, then apply one fix in the repo
argument-hint: "<what to look up>"
---
Use **Lane C** research tools — not scenarios or browser_* for this.

1. **web_search** with a precise query (framework + API/symbol names).
2. **fetch_web_page** on the best official URL if snippets are not enough.
3. **One** code or config change, then **verify** or **run_scenarios** as appropriate.
4. Do not search in a loop without editing or testing.

If tools say the API key is missing, tell the user: **/research setup**

Look up:
